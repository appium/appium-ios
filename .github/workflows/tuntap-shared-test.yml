name: Reusable tuntap native test

# Ported from appium-ios-tuntap's own shared.yml, adapted for the monorepo: installs run at
# the workspace root (shared lockfile) rather than inside packages/tuntap, and test/lint
# commands are scoped to the tuntap workspace. TS/JS lint and format:check are NOT
# re-run here - build.yml's lint job already covers packages/tuntap via the shared root
# oxlint/oxfmt config. Only the C++-specific checks (clang-format/clang-tidy), which have
# no equivalent elsewhere in the monorepo, are exposed as inputs here.

on:
  workflow_call:
    inputs:
      image:
        description: Runner image
        required: true
        type: string
      node-version:
        description: Node.js version
        required: true
        type: string
      test-command:
        description: Test command to execute (npm script name, run via `-w packages/tuntap`)
        required: true
        type: string
      test-name:
        description: Human-readable test name
        required: true
        type: string
      run-cpp-format-check:
        description: Run C++ format check (clang-format) before tests
        required: false
        default: false
        type: boolean
      run-cpp-lint:
        description: Run C++ lint (clang-tidy) before tests
        required: false
        default: false
        type: boolean

jobs:
  test:
    runs-on: ${{ inputs.image }}
    steps:
      - uses: actions/checkout@v7

      - name: Use Node.js ${{ inputs.node-version }}
        uses: actions/setup-node@v7
        with:
          node-version: ${{ inputs.node-version }}

      - name: Install Linux dependencies
        if: ${{ contains(inputs.image, 'ubuntu') }}
        run: |
          sudo apt-get update
          if ! sudo apt-get install -y linux-headers-$(uname -r); then
            echo "Specific kernel headers not found, installing generic headers"
            sudo apt-get install -y linux-headers-generic
          fi
          sudo apt-get install -y iproute2
          sudo modprobe tun

      - name: Configure Windows vcpkg cache
        if: ${{ contains(inputs.image, 'windows') }}
        shell: pwsh
        run: |
          "VCPKG_DEFAULT_BINARY_CACHE=$env:RUNNER_TEMP\vcpkg-binary-cache" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          New-Item -ItemType Directory -Force -Path "$env:RUNNER_TEMP\vcpkg-binary-cache" | Out-Null

      - name: Restore Windows vcpkg binary cache
        if: ${{ contains(inputs.image, 'windows') }}
        uses: actions/cache@v6
        with:
          path: ${{ runner.temp }}\vcpkg-binary-cache
          key: ${{ runner.os }}-vcpkg-openssl-x64-${{ hashFiles('.github/workflows/tuntap-shared-test.yml') }}
          restore-keys: |
            ${{ runner.os }}-vcpkg-openssl-x64-

      - name: Install Windows OpenSSL
        if: ${{ contains(inputs.image, 'windows') }}
        shell: pwsh
        run: |
          vcpkg install openssl:x64-windows-static
          "OPENSSL_ROOT_DIR=$env:VCPKG_INSTALLATION_ROOT\installed\x64-windows-static" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - uses: SocketDev/action@v1
        if: ${{ !contains(inputs.image, 'windows') }}
        with:
          mode: firewall-free

      - name: Install npm dependencies
        if: ${{ !contains(inputs.image, 'windows') }}
        run: sfw npm ci

      - name: Install npm dependencies
        if: ${{ contains(inputs.image, 'windows') }}
        run: npm ci

      - name: Install clang-format
        if: ${{ contains(inputs.image, 'ubuntu') && inputs.run-cpp-format-check }}
        run: sudo apt-get install -y clang-format

      - name: Install clang-tidy
        if: ${{ contains(inputs.image, 'ubuntu') && inputs.run-cpp-lint }}
        run: sudo apt-get install -y clang-tidy

      - name: Install clang-tidy (macOS)
        if: ${{ contains(inputs.image, 'macos') && inputs.run-cpp-lint }}
        run: |
          brew install llvm@22
          echo "$(brew --prefix llvm@22)/bin" >> "$GITHUB_PATH"

      - name: Install clang-tidy (Windows)
        if: ${{ contains(inputs.image, 'windows') && inputs.run-cpp-lint }}
        shell: pwsh
        run: |
          if (-not (Get-Command clang-tidy -ErrorAction SilentlyContinue)) {
            choco install llvm -y --no-progress
            "C:\Program Files\LLVM\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          }
          clang-tidy --version

      - name: Run C++ format check
        if: ${{ inputs.run-cpp-format-check }}
        run: npm run format:cpp:check --workspace packages/tuntap

      - name: Run C++ lint
        if: ${{ inputs.run-cpp-lint }}
        run: npm run lint:cpp --workspace packages/tuntap

      - name: Check IPv6 configuration on Linux
        if: ${{ contains(inputs.image, 'ubuntu') }}
        run: |
          echo "Checking IPv6 configuration..."
          ip -6 addr
          sysctl net.ipv6.conf.all.disable_ipv6
          sudo sysctl -w net.ipv6.conf.all.disable_ipv6=0
          sudo sysctl -w net.ipv6.conf.default.disable_ipv6=0
          sudo sysctl -w net.ipv6.conf.lo.disable_ipv6=0

      - name: Check Linux prerequisites
        if: ${{ contains(inputs.image, 'ubuntu') }}
        run: sudo bash packages/tuntap/test/check-linux-prereqs.sh

      - name: Run ${{ inputs.test-name }}
        if: ${{ !contains(inputs.image, 'windows') }}
        run: |
          set +e
          sudo npm run ${{ inputs.test-command }} --workspace packages/tuntap
          TEST_EXIT_CODE=$?

          if [ $TEST_EXIT_CODE -ne 0 ]; then
            echo "${{ inputs.test-name }} failed with exit code $TEST_EXIT_CODE"
            if [ "$RUNNER_OS" = "Linux" ]; then
              echo "Collecting Linux diagnostic information..."
              sudo lsmod | grep tun
              ls -la /dev/net/tun
              ip link
              ip -6 addr
            else
              echo "No additional diagnostics configured for $RUNNER_OS"
            fi
          fi

          exit $TEST_EXIT_CODE

      - name: Run ${{ inputs.test-name }}
        if: ${{ contains(inputs.image, 'windows') }}
        run: npm run ${{ inputs.test-command }} --workspace packages/tuntap
